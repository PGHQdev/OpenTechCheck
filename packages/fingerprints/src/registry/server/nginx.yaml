name: Nginx
slug: nginx
category: server
website: https://nginx.org
detect:
  headers:
    server:
# Passenger appends to the token: "nginx/1.18.0 + Phusion Passenger(R) 6.0.20",
# so the value must not be end-anchored.
      - pattern: '^nginx(?:/([\d.]+))?(?:$|\s)'
        version: 1
